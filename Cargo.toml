[package]
name = "pippo"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = "2"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
